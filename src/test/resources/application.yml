spring:
    application:
        name: mvc-starter
    sleuth:
        sampler:
            probability: 1.0

aurora:
    mvc:
        header:
            resttemplate:
                interceptor:
                    enabled: true

server:
    shutdown: immediate

management:
    server:
        port: -1
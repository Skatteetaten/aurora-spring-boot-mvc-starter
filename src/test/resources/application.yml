spring:
    application:
        name: mvc-starter

aurora:
    mvc:
        header:
            resttemplate:
                interceptor:
                    enabled: true

server:
    shutdown: immediate

management:
    server:
        port: -1
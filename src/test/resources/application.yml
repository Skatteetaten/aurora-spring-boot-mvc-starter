spring:
    application:
        name: mvc-starter

aurora:
    mvc:
        header:
            resttemplate:
                interceptor:
                    enabled: true
            span:
                interceptor:
                    enabled: true

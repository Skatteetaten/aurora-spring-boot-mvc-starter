spring:
    application:
        name: mvc-starter

aurora:
    mvc:
        header:
            resttemplate:
                interceptor:
                    enabled: true

openshift:
    cluster: local

pod:
    name: test-123
    namespace: test-dev

server:
    shutdown: immediate

management:
    server:
        port: -1
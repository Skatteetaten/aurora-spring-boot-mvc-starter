spring:
    application:
        name: mvc-starter
    zipkin:
        enabled: false

aurora:
    mvc:
        header:
            resttemplate:
                interceptor:
                    enabled: true

spring:
    application:
        name: mvc-starter

aurora:
    mvc:
        header:
            resttemplate:
                interceptor:
                    enabled: true

management:
    server:
        port: -1